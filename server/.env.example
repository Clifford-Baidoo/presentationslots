DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
EVENT_NAME="Presentation Slot Booking"
PORT=3001
COOKIE_SECRET="change-me-too"
# Not required for the single-Vercel-project setup (client + api are same-origin there).
# Only needed if the client is ever hosted on a different origin than the API.
CLIENT_ORIGIN=
